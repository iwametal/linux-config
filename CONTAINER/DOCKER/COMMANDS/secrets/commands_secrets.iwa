docker secret create [nome do secret] [arquivo]
		* cria uma nova secret.
		* os secrets, dentro dos containers, se localizam em /run/secrets

docker secret inspect [nome da secret]
		* traz informaćões sobre a secret especificada

docker secret rm [nome da secret]
		* deleta a secret especificada
